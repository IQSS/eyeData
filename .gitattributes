# Set the default behaviour, in case people don't have core.autocrlf set
* text=auto

# Explicitly declare text files you want to always be normalized an converted
# to native line endings on checkout
*.c text
*.h text

# Declare files that will always have CRLF line endings on checkout
*.sln text eol=crlf

# Denonte all files that are turly binary and should not be modified
*.png binary
*.jpg binary
